{% extends 'base.html.twig' %}

{% block title %}Inicio - Sushi Moderno{% endblock %}

{% block body %}
   <!-- Hero Carousel Mejorado -->
<section class="sushi-hero" id="inicio">
    <div class="sushi-carousel">
        <!-- Slide 1 con texto promocional -->
        <div class="carousel-slide slide-1 active">
            <div class="slide-content">
                <span class="slide-badge">⭐ Especialidad de la Casa</span>
                <h2 class="slide-title">Sashimi Premium<br>Frescura Garantizada</h2>
                <p class="slide-subtitle">Cortes perfectos de salmón, atún y lubina. Pescado fresco del día.</p>
            </div>
        </div>
        
        <!-- Slide 2 con texto promocional -->
        <div class="carousel-slide slide-2">
            <div class="slide-content">
                <span class="slide-badge">🔥 Más Vendido</span>
                <h2 class="slide-title">Mexican Roll<br>Explosión de Sabores</h2>
                <p class="slide-subtitle">Camarón tempura, aguacate y nuestra salsa especial unagi.</p>
            </div>
        </div>
        
        <!-- Slide 3 con texto promocional -->
        <div class="carousel-slide slide-3">
            <div class="slide-content">
                <span class="slide-badge">👨‍🍳 Chef's Selection</span>
                <h2 class="slide-title">Nigiri Ebby<br>Selección Exclusiva</h2>
                <p class="slide-subtitle">10 piezas de pura perfección: salmón, atún, camarón y más.</p>
            </div>
        </div>
    </div>
    
    <!-- Contenido Principal del Hero -->
    <div class="sushi-hero-content">
        <div class="container">
            <div class="hero-text animate-fade-in-up">
                <!-- LOGO EN HERO -->
                <div class="hero-logo">
                    <img src="{{ asset('images/products/Logo.png') }}" alt="HUGO - The Best Sushi" class="hero-logo-img">
                </div>
                
                <div class="hero-badge badge-primary">
                    <i class="fas fa-star me-2"></i>Experiencia Premium
                </div>
                
                <h1 class="hero-title">
                    Auténtico Sushi<br>
                    <span style="color: var(--sushi-red);">Japonés</span> en tu Ciudad
                </h1>
                <p class="hero-subtitle">
                    Descubre la perfección en cada bocado. Ingredientes frescos, 
                    técnicas tradicionales y sabores que transportan a Tokio.
                </p>
                <div class="hero-buttons">
                    <a href="#menu" class="sushi-btn sushi-btn-primary">
                        <i class="fas fa-utensils"></i>
                        Ver Menú
                    </a>
                    <a href="#contacto" class="sushi-btn sushi-btn-outline">
                        <i class="fas fa-phone"></i>
                        Reservar
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Indicadores del Carousel -->
    <div class="carousel-indicators">
        <button class="carousel-indicator active" data-slide="0"></button>
        <button class="carousel-indicator" data-slide="1"></button>
        <button class="carousel-indicator" data-slide="2"></button>
    </div>
</section>

    <!-- Sección Especialidades -->
    <section class="sushi-section" id="menu">
        <div class="container">
            <div class="section-header">
                <span class="section-badge">Especialidades</span>
                <h2 class="section-title">Nuestras Creaciones</h2>
                <p class="section-subtitle">
                    Cada pieza es una obra de arte, preparada con los ingredientes 
                    más frescos y técnicas milenarias.
                </p>
            </div>

            <div class="products-grid">
                <div class="product-card">
                    <div class="product-image" style="background-image: url('/images/products/Sashimi.png');"></div>
                    <div class="product-content">
                        <h3 class="product-title">Rolls</h3>
                        <p class="product-description">
                            De lo clásico a lo creativo. Descubrí nuestra irresistible 
                            variedad de makis y uramakis 
                            súper frescos, hechos al momento.
                        </p>
                     </div>
                </div>

                <div class="product-card">
                    <div class="product-image" style="background-image: url('/images/products/MexicanRoll.png');"></div>
                    <div class="product-content">
                        <h3 class="product-title">Combinados</h3>
                        <p class="product-description">
                            El mix perfecto. Selecciones del chef 
                            con nuestras mejores piezas, ideales para probar 
                            de todo o para compartir.
                        </p>
                    </div>
                </div>

                <div class="product-card">
                    <div class="product-image" style="background-image: url('/images/products/Nigiriebby.png');"></div>
                    <div class="product-content">
                        <h3 class="product-title">Especialidades</h3>
                        <p class="product-description">
                            Sabor de autor. Déjate sorprender por nuestras 
                            tradicionales harumakis y también opciones unicas 
                            que no encontrarás en otro lugar
                        </p>
                     </div>
                </div>
            </div>
        </div>
    </section>

     <script>
document.addEventListener('DOMContentLoaded', function() {
    // Carousel
    const slides = document.querySelectorAll('.carousel-slide');
    let currentSlide = 0;

    function showSlide(n) {
        slides.forEach(slide => slide.classList.remove('active'));
        currentSlide = (n + slides.length) % slides.length;
        slides[currentSlide].classList.add('active');
    }

    function nextSlide() {
        showSlide(currentSlide + 1);
    }

    setInterval(nextSlide, 5000);

    // Scroll suave para links internos
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });

    // Navbar sólida al hacer scroll
    const nav = document.querySelector('.sushi-nav');
    document.addEventListener('scroll', () => {
        if (window.scrollY > 50) {
            nav.classList.add('scrolled');
        } else {
            nav.classList.remove('scrolled');
        }
    });
});
</script>
{% endblock %}